# Sample GAE flexible project

## Develop 

`cp .env.example .env`

Get JSON file with GCP service account creds, paste path to `.env`

`docker-compose up -d`

## Deploy

`gcloud app deploy --version YOUR-VERSION`

